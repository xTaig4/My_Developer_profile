'use client';

const ProfilePage = () => {
    return (
        <div className="flex  items-center justify-center h-screen bg-gray-800">
            Profile page
        </div>
    );
};

export default ProfilePage;